{
  "name": "langgraph-insurance-orchestrator",
  "version": "0.1.0",
  "description": "LangGraph orchestrator for insurance homepage with Claude Code CLI subprocess coordination",
  "requires-python": {"python": ">=3.11"},
  "dependencies": {
    "langgraph": "^0.2.0",
    "langchain-core": "^0.3.0",
    "langchain-anthropic": "^0.2.0",
    "anthropic": ">=0.30.0",
    "gitpython": ">=3.1.40",
    "pydantic": ">=2.0.0",
    "python-dotenv": ">=1.0.0",
    "tqdm": ">=4.66.0",
    "rich": ">=13.7.0"
  },
  "dev-dependencies": {
    "pytest": ">=8.0.0",
    "pytest-asyncio": ">=0.23.0",
    "ruff": ">=0.4.0"
  },
  "scripts": {
    "run": "python -m src.main",
    "dev": "python -m src.main --dev",
    "test": "pytest tests/ -v"
  }
}